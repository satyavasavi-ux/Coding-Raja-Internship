# feat: add automated ONNX model export and TensorRT inference compiler

## Summary
- Adds automated ONNX computation graph serialization and quantization (INT8/FP16).
- Implements TensorRT inference engine compiler with low-latency execution benchmarks.
- Adds model format converter UI component and integration test suite.
